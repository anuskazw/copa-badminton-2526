---
import Layout from '../layouts/Layout.astro';

// DOBLES FEMENINO
const doblesParticipantes = [
  { letra: 'A', nombre: '' },
  { letra: 'B', nombre: '' },
  { letra: 'C', nombre: '' },
  { letra: 'D', nombre: '' }
];

// INDIVIDUAL FEMENINO
const individualParticipantes = [
  { letra: 'A', nombre: '' },
  { letra: 'B', nombre: '' },
  { letra: 'C', nombre: '' },
  { letra: 'D', nombre: '' },
  { letra: 'E', nombre: '' }
];
---

<Layout title="Copa de Bádminton FMDS 2025/2026">
  <div class="info">
    <p><strong>📍 Lugar:</strong> CDM La Elipa</p>
    <p><strong>🕐 Horario:</strong> 10:00 - 15:00</p>
    <p><strong>📋 Formato:</strong> 2 sets a 21 · Tercer set a 15 si hay empate</p>
  </div>

  <!-- DOBLES FEMENINO -->
  <section class="modalidad modalidad-dobles" id="dobles-femenino">
    <h2>
      <span>Dobles Femenino</span>
      <button class="btn-print-section" data-section="dobles-femenino" title="Imprimir cuadro">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <polyline points="6,9 6,2 18,2 18,9"></polyline>
          <path d="M6,18 L4,18 C2.9,18 2,17.1 2,16 L2,11 C2,9.9 2.9,9 4,9 L20,9 C21.1,9 22,9.9 22,11 L22,16 C22,17.1 21.1,18 20,18 L18,18"></path>
          <rect x="6" y="14" width="12" height="8"></rect>
        </svg>
      </button>
    </h2>

    <h3>Participantes</h3>
    <div class="participantes-grid">
      {doblesParticipantes.map(p => (
        <div class="participante-tag">
          <span class="letra">{p.letra}</span>
          <input type="text" class="participante-input" data-participante={`dobles-${p.letra}`} value={p.nombre} />
        </div>
      ))}
    </div>

    <h3>Cuadro de Eliminatorias</h3>
    <div class="bracket-container">
      <div class="bracket">
        <div class="bracket-round">
          <div class="round-label">Semifinales</div>
          <div class="match" data-match>
            <div class="match-team" data-team="1">
              <span class="team-name" data-show="dobles-A">{doblesParticipantes[0].nombre}</span>
              <div class="score-total"><span data-sets-won>-</span></div>
              <div class="score-boxes">
                <input type="text" class="score-input" maxlength="2" data-set="1" />
                <input type="text" class="score-input" maxlength="2" data-set="2" />
                <input type="text" class="score-input third" maxlength="2" data-set="3" />
              </div>
            </div>
            <div class="match-team" data-team="2">
              <span class="team-name" data-show="dobles-D">{doblesParticipantes[3].nombre}</span>
              <div class="score-total"><span data-sets-won>-</span></div>
              <div class="score-boxes">
                <input type="text" class="score-input" maxlength="2" data-set="1" />
                <input type="text" class="score-input" maxlength="2" data-set="2" />
                <input type="text" class="score-input third" maxlength="2" data-set="3" />
              </div>
            </div>
          </div>
          <div class="match" data-match>
            <div class="match-team" data-team="1">
              <span class="team-name" data-show="dobles-B">{doblesParticipantes[1].nombre}</span>
              <div class="score-total"><span data-sets-won>-</span></div>
              <div class="score-boxes">
                <input type="text" class="score-input" maxlength="2" data-set="1" />
                <input type="text" class="score-input" maxlength="2" data-set="2" />
                <input type="text" class="score-input third" maxlength="2" data-set="3" />
              </div>
            </div>
            <div class="match-team" data-team="2">
              <span class="team-name" data-show="dobles-C">{doblesParticipantes[2].nombre}</span>
              <div class="score-total"><span data-sets-won>-</span></div>
              <div class="score-boxes">
                <input type="text" class="score-input" maxlength="2" data-set="1" />
                <input type="text" class="score-input" maxlength="2" data-set="2" />
                <input type="text" class="score-input third" maxlength="2" data-set="3" />
              </div>
            </div>
          </div>
        </div>

        <div class="bracket-connector">
          <div class="connector-line top"></div>
          <div class="connector-line bottom"></div>
          <div class="connector-merge"></div>
        </div>

        <div class="bracket-round">
          <div class="round-label">Final</div>
          <div class="match final-match" data-match>
            <div class="match-team" data-team="1">
              <input type="text" class="team-name-input" data-finalist="dobles-sf1" placeholder="Gan. SF1" />
              <div class="score-total"><span data-sets-won>-</span></div>
              <div class="score-boxes">
                <input type="text" class="score-input" maxlength="2" data-set="1" />
                <input type="text" class="score-input" maxlength="2" data-set="2" />
                <input type="text" class="score-input third" maxlength="2" data-set="3" />
              </div>
            </div>
            <div class="match-team" data-team="2">
              <input type="text" class="team-name-input" data-finalist="dobles-sf2" placeholder="Gan. SF2" />
              <div class="score-total"><span data-sets-won>-</span></div>
              <div class="score-boxes">
                <input type="text" class="score-input" maxlength="2" data-set="1" />
                <input type="text" class="score-input" maxlength="2" data-set="2" />
                <input type="text" class="score-input third" maxlength="2" data-set="3" />
              </div>
            </div>
          </div>
        </div>

      </div>

      <div class="tercer-puesto">
        <div class="round-label">3er - 4º Puesto</div>
        <div class="match" data-match>
          <div class="match-team" data-team="1">
            <input type="text" class="team-name-input" data-finalist="dobles-perd1" placeholder="Perd. SF1" />
            <div class="score-total"><span data-sets-won>-</span></div>
            <div class="score-boxes">
              <input type="text" class="score-input" maxlength="2" data-set="1" />
              <input type="text" class="score-input" maxlength="2" data-set="2" />
              <input type="text" class="score-input third" maxlength="2" data-set="3" />
            </div>
          </div>
          <div class="match-team" data-team="2">
            <input type="text" class="team-name-input" data-finalist="dobles-perd2" placeholder="Perd. SF2" />
            <div class="score-total"><span data-sets-won>-</span></div>
            <div class="score-boxes">
              <input type="text" class="score-input" maxlength="2" data-set="1" />
              <input type="text" class="score-input" maxlength="2" data-set="2" />
              <input type="text" class="score-input third" maxlength="2" data-set="3" />
            </div>
          </div>
        </div>
      </div>
    </div>

    <h3>Clasificación Final</h3>
    <table class="clasificacion" data-clasificacion="dobles">
      <thead>
        <tr>
          <th>Pos.</th>
          <th>Participante</th>
          <th>PJ</th>
          <th>PG</th>
          <th>PP</th>
          <th>Sets +</th>
          <th>Sets -</th>
          <th>Diff</th>
        </tr>
      </thead>
      <tbody>
        <tr><td>1º</td><td>-</td><td>-</td><td>-</td><td>-</td><td>-</td><td>-</td><td>-</td></tr>
        <tr><td>2º</td><td>-</td><td>-</td><td>-</td><td>-</td><td>-</td><td>-</td><td>-</td></tr>
        <tr><td>3º</td><td>-</td><td>-</td><td>-</td><td>-</td><td>-</td><td>-</td><td>-</td></tr>
        <tr><td>4º</td><td>-</td><td>-</td><td>-</td><td>-</td><td>-</td><td>-</td><td>-</td></tr>
      </tbody>
    </table>
  </section>

  <!-- INDIVIDUAL FEMENINO -->
  <section class="modalidad modalidad-individual" id="individual-femenino">
    <h2>
      <span>Individual Femenino</span>
      <button class="btn-print-section" data-section="individual-femenino" title="Imprimir cuadro">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <polyline points="6,9 6,2 18,2 18,9"></polyline>
          <path d="M6,18 L4,18 C2.9,18 2,17.1 2,16 L2,11 C2,9.9 2.9,9 4,9 L20,9 C21.1,9 22,9.9 22,11 L22,16 C22,17.1 21.1,18 20,18 L18,18"></path>
          <rect x="6" y="14" width="12" height="8"></rect>
        </svg>
      </button>
    </h2>

    <h3>Participantes</h3>
    <div class="participantes-grid">
      {individualParticipantes.map(p => (
        <div class="participante-tag">
          <span class="letra">{p.letra}</span>
          <input type="text" class="participante-input" data-participante={`individual-${p.letra}`} value={p.nombre} />
        </div>
      ))}
    </div>

    <h3>Cuadro de Eliminatorias</h3>
    <div class="bracket-container">
      <div class="bracket">
        <div class="bracket-round">
          <div class="round-label">Primera Ronda</div>
          <div class="match" data-match>
            <div class="match-team" data-team="1">
              <span class="team-name" data-show="individual-D">{individualParticipantes[3].nombre}</span>
              <div class="score-total"><span data-sets-won>-</span></div>
              <div class="score-boxes">
                <input type="text" class="score-input" maxlength="2" data-set="1" />
                <input type="text" class="score-input" maxlength="2" data-set="2" />
                <input type="text" class="score-input third" maxlength="2" data-set="3" />
              </div>
            </div>
            <div class="match-team" data-team="2">
              <span class="team-name" data-show="individual-E">{individualParticipantes[4].nombre}</span>
              <div class="score-total"><span data-sets-won>-</span></div>
              <div class="score-boxes">
                <input type="text" class="score-input" maxlength="2" data-set="1" />
                <input type="text" class="score-input" maxlength="2" data-set="2" />
                <input type="text" class="score-input third" maxlength="2" data-set="3" />
              </div>
            </div>
          </div>
          <div class="bye-box">
            <span>A, B, C pasan directas a semifinales</span>
          </div>
        </div>

        <div class="bracket-connector single">
          <div class="connector-line middle"></div>
        </div>

        <div class="bracket-round">
          <div class="round-label">Semifinales</div>
          <div class="match" data-match>
            <div class="match-team" data-team="1">
              <span class="team-name" data-show="individual-A">{individualParticipantes[0].nombre}</span>
              <div class="score-total"><span data-sets-won>-</span></div>
              <div class="score-boxes">
                <input type="text" class="score-input" maxlength="2" data-set="1" />
                <input type="text" class="score-input" maxlength="2" data-set="2" />
                <input type="text" class="score-input third" maxlength="2" data-set="3" />
              </div>
            </div>
            <div class="match-team" data-team="2">
              <input type="text" class="team-name-input" data-finalist="individual-gan-de" placeholder="Gan. D/E" />
              <div class="score-total"><span data-sets-won>-</span></div>
              <div class="score-boxes">
                <input type="text" class="score-input" maxlength="2" data-set="1" />
                <input type="text" class="score-input" maxlength="2" data-set="2" />
                <input type="text" class="score-input third" maxlength="2" data-set="3" />
              </div>
            </div>
          </div>
          <div class="match" data-match>
            <div class="match-team" data-team="1">
              <span class="team-name" data-show="individual-B">{individualParticipantes[1].nombre}</span>
              <div class="score-total"><span data-sets-won>-</span></div>
              <div class="score-boxes">
                <input type="text" class="score-input" maxlength="2" data-set="1" />
                <input type="text" class="score-input" maxlength="2" data-set="2" />
                <input type="text" class="score-input third" maxlength="2" data-set="3" />
              </div>
            </div>
            <div class="match-team" data-team="2">
              <span class="team-name" data-show="individual-C">{individualParticipantes[2].nombre}</span>
              <div class="score-total"><span data-sets-won>-</span></div>
              <div class="score-boxes">
                <input type="text" class="score-input" maxlength="2" data-set="1" />
                <input type="text" class="score-input" maxlength="2" data-set="2" />
                <input type="text" class="score-input third" maxlength="2" data-set="3" />
              </div>
            </div>
          </div>
        </div>

        <div class="bracket-connector">
          <div class="connector-line top"></div>
          <div class="connector-line bottom"></div>
          <div class="connector-merge"></div>
        </div>

        <div class="bracket-round">
          <div class="round-label">Final</div>
          <div class="match final-match" data-match>
            <div class="match-team" data-team="1">
              <input type="text" class="team-name-input" data-finalist="individual-sf1" placeholder="Gan. SF1" />
              <div class="score-total"><span data-sets-won>-</span></div>
              <div class="score-boxes">
                <input type="text" class="score-input" maxlength="2" data-set="1" />
                <input type="text" class="score-input" maxlength="2" data-set="2" />
                <input type="text" class="score-input third" maxlength="2" data-set="3" />
              </div>
            </div>
            <div class="match-team" data-team="2">
              <input type="text" class="team-name-input" data-finalist="individual-sf2" placeholder="Gan. SF2" />
              <div class="score-total"><span data-sets-won>-</span></div>
              <div class="score-boxes">
                <input type="text" class="score-input" maxlength="2" data-set="1" />
                <input type="text" class="score-input" maxlength="2" data-set="2" />
                <input type="text" class="score-input third" maxlength="2" data-set="3" />
              </div>
            </div>
          </div>
        </div>

      </div>

      <div class="puestos-inferiores">
        <div class="tercer-puesto">
          <div class="round-label">3er - 4º Puesto</div>
          <div class="match" data-match>
            <div class="match-team" data-team="1">
              <input type="text" class="team-name-input" data-finalist="individual-perd1" placeholder="Perd. SF1" />
              <div class="score-total"><span data-sets-won>-</span></div>
              <div class="score-boxes">
                <input type="text" class="score-input" maxlength="2" data-set="1" />
                <input type="text" class="score-input" maxlength="2" data-set="2" />
                <input type="text" class="score-input third" maxlength="2" data-set="3" />
              </div>
            </div>
            <div class="match-team" data-team="2">
              <input type="text" class="team-name-input" data-finalist="individual-perd2" placeholder="Perd. SF2" />
              <div class="score-total"><span data-sets-won>-</span></div>
              <div class="score-boxes">
                <input type="text" class="score-input" maxlength="2" data-set="1" />
                <input type="text" class="score-input" maxlength="2" data-set="2" />
                <input type="text" class="score-input third" maxlength="2" data-set="3" />
              </div>
            </div>
          </div>
        </div>

        <div class="quinto-puesto">
          <div class="round-label">5º Puesto</div>
          <div class="match bye-match">
            <div class="match-team">
              <input type="text" class="team-name-input" data-finalist="individual-perd-de" placeholder="Perdedora D vs E" />
            </div>
          </div>
        </div>
      </div>
    </div>

    <h3>Clasificación Final</h3>
    <table class="clasificacion" data-clasificacion="individual">
      <thead>
        <tr>
          <th>Pos.</th>
          <th>Participante</th>
          <th>PJ</th>
          <th>PG</th>
          <th>PP</th>
          <th>Sets +</th>
          <th>Sets -</th>
          <th>Diff</th>
        </tr>
      </thead>
      <tbody>
        <tr><td>1º</td><td>-</td><td>-</td><td>-</td><td>-</td><td>-</td><td>-</td><td>-</td></tr>
        <tr><td>2º</td><td>-</td><td>-</td><td>-</td><td>-</td><td>-</td><td>-</td><td>-</td></tr>
        <tr><td>3º</td><td>-</td><td>-</td><td>-</td><td>-</td><td>-</td><td>-</td><td>-</td></tr>
        <tr><td>4º</td><td>-</td><td>-</td><td>-</td><td>-</td><td>-</td><td>-</td><td>-</td></tr>
        <tr><td>5º</td><td>-</td><td>-</td><td>-</td><td>-</td><td>-</td><td>-</td><td>-</td></tr>
      </tbody>
    </table>
  </section>

  <section class="sin-participacion">
    <p>Sin participación: Dobles Mixto · Dobles Masculino · Individual Masculino</p>
  </section>

  <!-- PROGRAMACIÓN -->
  <section class="programacion" id="programacion">
    <h2>
      <span>Programación</span>
      <div class="programacion-controls">
        <label class="toggle-visibility" title="Mostrar/ocultar en página pública">
          <input type="checkbox" id="toggle-programacion-visible" checked />
          <span class="toggle-slider"></span>
          <span class="toggle-label">Visible</span>
        </label>
        <button class="btn-add-partido" id="btn-add-partido" title="Añadir partido">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <line x1="12" y1="5" x2="12" y2="19"></line>
            <line x1="5" y1="12" x2="19" y2="12"></line>
          </svg>
        </button>
      </div>
    </h2>
    <div class="tabla-programacion-container">
      <table class="tabla-programacion" id="tabla-programacion">
        <thead>
          <tr>
            <th class="drag-handle-header"></th>
            <th>Enfrentamiento</th>
            <th>Modalidad</th>
            <th>Pista</th>
            <th>Hora</th>
            <th class="delete-header"></th>
          </tr>
        </thead>
        <tbody id="programacion-tbody">
          <!-- Los partidos se añaden dinámicamente -->
        </tbody>
      </table>
    </div>
    <p class="programacion-hint">Arrastra las filas para reordenar los partidos</p>
  </section>
</Layout>

<script>
  function calculateSetsWon() {
    document.querySelectorAll('[data-match]').forEach(match => {
      const team1 = match.querySelector('[data-team="1"]');
      const team2 = match.querySelector('[data-team="2"]');

      if (!team1 || !team2) return;

      let sets1 = 0;
      let sets2 = 0;

      for (let i = 1; i <= 3; i++) {
        const input1 = team1.querySelector(`[data-set="${i}"]`);
        const input2 = team2.querySelector(`[data-set="${i}"]`);

        if (!input1 || !input2) continue;

        const score1 = parseInt(input1.value) || 0;
        const score2 = parseInt(input2.value) || 0;

        if (score1 > 0 || score2 > 0) {
          if (score1 > score2) sets1++;
          else if (score2 > score1) sets2++;
        }
      }

      const display1 = team1.querySelector('[data-sets-won]');
      const display2 = team2.querySelector('[data-sets-won]');

      if (display1) display1.textContent = sets1 > 0 || sets2 > 0 ? sets1 : '-';
      if (display2) display2.textContent = sets1 > 0 || sets2 > 0 ? sets2 : '-';
    });
  }

  document.querySelectorAll('[data-set]').forEach(input => {
    input.addEventListener('input', calculateSetsWon);
  });

  // Autocompletar finalistas basándose en resultados de semifinales
  function updateFinalists() {
    // DOBLES FEMENINO
    const doblesMatches = Array.from(document.querySelectorAll('#dobles-femenino [data-match]'));
    // Partidos: 0=SF1, 1=SF2, 2=Final, 3=3erPuesto
    const doblesSF1 = doblesMatches[0];
    const doblesSF2 = doblesMatches[1];

    if (doblesSF1) {
      const result = getMatchResult(doblesSF1);
      if (result?.played && result.winner !== 0) {
        const winner = result.winner === 1 ? result.team1.name : result.team2.name;
        const loser = result.winner === 1 ? result.team2.name : result.team1.name;
        const ganInput = document.querySelector('[data-finalist="dobles-sf1"]');
        const perdInput = document.querySelector('[data-finalist="dobles-perd1"]');
        if (ganInput && winner) ganInput.value = winner;
        if (perdInput && loser) perdInput.value = loser;
      }
    }

    if (doblesSF2) {
      const result = getMatchResult(doblesSF2);
      if (result?.played && result.winner !== 0) {
        const winner = result.winner === 1 ? result.team1.name : result.team2.name;
        const loser = result.winner === 1 ? result.team2.name : result.team1.name;
        const ganInput = document.querySelector('[data-finalist="dobles-sf2"]');
        const perdInput = document.querySelector('[data-finalist="dobles-perd2"]');
        if (ganInput && winner) ganInput.value = winner;
        if (perdInput && loser) perdInput.value = loser;
      }
    }

    // INDIVIDUAL FEMENINO
    const indMatches = Array.from(document.querySelectorAll('#individual-femenino [data-match]'));
    // Partidos: 0=Primera Ronda (D vs E), 1=SF1, 2=SF2, 3=Final, 4=3erPuesto
    const indPrimeraRonda = indMatches[0];
    const indSF1 = indMatches[1];
    const indSF2 = indMatches[2];

    // Primera ronda: ganador va a SF1, perdedor va a 5º puesto
    if (indPrimeraRonda) {
      const result = getMatchResult(indPrimeraRonda);
      if (result?.played && result.winner !== 0) {
        const winner = result.winner === 1 ? result.team1.name : result.team2.name;
        const loser = result.winner === 1 ? result.team2.name : result.team1.name;
        const ganDEInput = document.querySelector('[data-finalist="individual-gan-de"]');
        const perdDEInput = document.querySelector('[data-finalist="individual-perd-de"]');
        if (ganDEInput && winner) ganDEInput.value = winner;
        if (perdDEInput && loser) perdDEInput.value = loser;
      }
    }

    if (indSF1) {
      const result = getMatchResult(indSF1);
      if (result?.played && result.winner !== 0) {
        const winner = result.winner === 1 ? result.team1.name : result.team2.name;
        const loser = result.winner === 1 ? result.team2.name : result.team1.name;
        const ganInput = document.querySelector('[data-finalist="individual-sf1"]');
        const perdInput = document.querySelector('[data-finalist="individual-perd1"]');
        if (ganInput && winner) ganInput.value = winner;
        if (perdInput && loser) perdInput.value = loser;
      }
    }

    if (indSF2) {
      const result = getMatchResult(indSF2);
      if (result?.played && result.winner !== 0) {
        const winner = result.winner === 1 ? result.team1.name : result.team2.name;
        const loser = result.winner === 1 ? result.team2.name : result.team1.name;
        const ganInput = document.querySelector('[data-finalist="individual-sf2"]');
        const perdInput = document.querySelector('[data-finalist="individual-perd2"]');
        if (ganInput && winner) ganInput.value = winner;
        if (perdInput && loser) perdInput.value = loser;
      }
    }
  }

  // Sincronizar nombres de participantes
  document.querySelectorAll('[data-participante]').forEach(input => {
    input.addEventListener('input', (e) => {
      const key = e.target.dataset.participante;
      document.querySelectorAll(`[data-show="${key}"]`).forEach(span => {
        span.textContent = e.target.value;
      });
      // Actualizar finalistas cuando cambian los nombres
      updateFinalists();
    });
  });

  // Imprimir sección individual en formato horizontal
  document.querySelectorAll('.btn-print-section').forEach(btn => {
    btn.addEventListener('click', (e) => {
      const sectionId = e.currentTarget.dataset.section;
      document.body.setAttribute('data-print-section', sectionId);
      window.print();
      document.body.removeAttribute('data-print-section');
    });
  });

  // Calcular clasificación
  function getMatchResult(match) {
    const team1 = match.querySelector('[data-team="1"]');
    const team2 = match.querySelector('[data-team="2"]');
    if (!team1 || !team2) return null;

    let sets1 = 0, sets2 = 0;
    let points1 = 0, points2 = 0;

    for (let i = 1; i <= 3; i++) {
      const input1 = team1.querySelector(`[data-set="${i}"]`);
      const input2 = team2.querySelector(`[data-set="${i}"]`);
      if (!input1 || !input2) continue;

      const score1 = parseInt(input1.value) || 0;
      const score2 = parseInt(input2.value) || 0;

      points1 += score1;
      points2 += score2;

      if (score1 > 0 || score2 > 0) {
        if (score1 > score2) sets1++;
        else if (score2 > score1) sets2++;
      }
    }

    const nameInput1 = team1.querySelector('.team-name-input');
    const nameInput2 = team2.querySelector('.team-name-input');
    const name1 = nameInput1 ? nameInput1.value : (team1.querySelector('.team-name')?.textContent || '');
    const name2 = nameInput2 ? nameInput2.value : (team2.querySelector('.team-name')?.textContent || '');

    return {
      team1: { name: name1, sets: sets1, points: points1 },
      team2: { name: name2, sets: sets2, points: points2 },
      winner: sets1 > sets2 ? 1 : sets2 > sets1 ? 2 : 0,
      played: sets1 > 0 || sets2 > 0
    };
  }

  function updateClasificacion() {
    // DOBLES FEMENINO - Clasificación por posición en bracket
    const doblesSection = document.getElementById('dobles-femenino');
    const doblesMatches = Array.from(doblesSection.querySelectorAll('[data-match]'));
    const doblesTable = doblesSection.querySelector('[data-clasificacion="dobles"] tbody');

    // Partidos: 0=SF1, 1=SF2, 2=Final, 3=3erPuesto
    const finalMatch = doblesMatches[2];
    const tercerMatch = doblesMatches[3];

    const doblesStats = {};
    const initStats = (name) => {
      if (name && !doblesStats[name] && !name.includes('Gan.') && !name.includes('Perd.')) {
        doblesStats[name] = { pj: 0, pg: 0, pp: 0, setsF: 0, setsC: 0, posicion: 99 };
      }
    };

    // Procesar todos los partidos para estadísticas
    doblesMatches.forEach(match => {
      const result = getMatchResult(match);
      if (!result || !result.played) return;

      const { team1, team2, winner } = result;
      initStats(team1.name);
      initStats(team2.name);

      if (doblesStats[team1.name]) {
        doblesStats[team1.name].pj++;
        doblesStats[team1.name].setsF += team1.sets;
        doblesStats[team1.name].setsC += team2.sets;
        if (winner === 1) doblesStats[team1.name].pg++;
        else if (winner === 2) doblesStats[team1.name].pp++;
      }

      if (doblesStats[team2.name]) {
        doblesStats[team2.name].pj++;
        doblesStats[team2.name].setsF += team2.sets;
        doblesStats[team2.name].setsC += team1.sets;
        if (winner === 2) doblesStats[team2.name].pg++;
        else if (winner === 1) doblesStats[team2.name].pp++;
      }
    });

    // Determinar posiciones por bracket
    const finalResult = getMatchResult(finalMatch);
    const tercerResult = getMatchResult(tercerMatch);

    if (finalResult?.played) {
      const winner = finalResult.winner === 1 ? finalResult.team1.name : finalResult.team2.name;
      const loser = finalResult.winner === 1 ? finalResult.team2.name : finalResult.team1.name;
      if (doblesStats[winner]) doblesStats[winner].posicion = 1;
      if (doblesStats[loser]) doblesStats[loser].posicion = 2;
    }

    if (tercerResult?.played) {
      const winner = tercerResult.winner === 1 ? tercerResult.team1.name : tercerResult.team2.name;
      const loser = tercerResult.winner === 1 ? tercerResult.team2.name : tercerResult.team1.name;
      if (doblesStats[winner]) doblesStats[winner].posicion = 3;
      if (doblesStats[loser]) doblesStats[loser].posicion = 4;
    }

    // Ordenar por posición en bracket, luego por PG
    const doblesRanking = Object.entries(doblesStats)
      .map(([name, stats]) => ({ name, ...stats, diff: stats.setsF - stats.setsC }))
      .sort((a, b) => a.posicion - b.posicion || b.pg - a.pg || b.diff - a.diff);

    const doblesRows = doblesTable.querySelectorAll('tr');
    doblesRows.forEach((row, i) => {
      const cells = row.querySelectorAll('td');
      if (doblesRanking[i]) {
        const p = doblesRanking[i];
        cells[1].textContent = p.name;
        cells[2].textContent = p.pj;
        cells[3].textContent = p.pg;
        cells[4].textContent = p.pp;
        cells[5].textContent = p.setsF;
        cells[6].textContent = p.setsC;
        cells[7].textContent = p.diff > 0 ? `+${p.diff}` : p.diff;
      } else {
        for (let j = 1; j < cells.length; j++) cells[j].textContent = '-';
      }
    });

    // INDIVIDUAL FEMENINO - Clasificación por posición en bracket
    const individualSection = document.getElementById('individual-femenino');
    const individualMatches = Array.from(individualSection.querySelectorAll('[data-match]'));
    const individualTable = individualSection.querySelector('[data-clasificacion="individual"] tbody');

    // Partidos: 0=Primera Ronda (D vs E), 1=SF1, 2=SF2, 3=Final, 4=3erPuesto
    const primeraRondaMatch = individualMatches[0];
    const indFinalMatch = individualMatches[3];
    const indTercerMatch = individualMatches[4];

    const individualStats = {};
    const initIndStats = (name) => {
      if (name && !individualStats[name] && !name.includes('Gan.') && !name.includes('Perd.')) {
        individualStats[name] = { pj: 0, pg: 0, pp: 0, setsF: 0, setsC: 0, posicion: 99 };
      }
    };

    // Procesar todos los partidos para estadísticas
    individualMatches.forEach(match => {
      const result = getMatchResult(match);
      if (!result || !result.played) return;

      const { team1, team2, winner } = result;
      initIndStats(team1.name);
      initIndStats(team2.name);

      if (individualStats[team1.name]) {
        individualStats[team1.name].pj++;
        individualStats[team1.name].setsF += team1.sets;
        individualStats[team1.name].setsC += team2.sets;
        if (winner === 1) individualStats[team1.name].pg++;
        else if (winner === 2) individualStats[team1.name].pp++;
      }

      if (individualStats[team2.name]) {
        individualStats[team2.name].pj++;
        individualStats[team2.name].setsF += team2.sets;
        individualStats[team2.name].setsC += team1.sets;
        if (winner === 2) individualStats[team2.name].pg++;
        else if (winner === 1) individualStats[team2.name].pp++;
      }
    });

    // Determinar posiciones por bracket
    const indFinalResult = getMatchResult(indFinalMatch);
    const indTercerResult = getMatchResult(indTercerMatch);
    const primeraRondaResult = getMatchResult(primeraRondaMatch);

    if (indFinalResult?.played) {
      const winner = indFinalResult.winner === 1 ? indFinalResult.team1.name : indFinalResult.team2.name;
      const loser = indFinalResult.winner === 1 ? indFinalResult.team2.name : indFinalResult.team1.name;
      if (individualStats[winner]) individualStats[winner].posicion = 1;
      if (individualStats[loser]) individualStats[loser].posicion = 2;
    }

    if (indTercerResult?.played) {
      const winner = indTercerResult.winner === 1 ? indTercerResult.team1.name : indTercerResult.team2.name;
      const loser = indTercerResult.winner === 1 ? indTercerResult.team2.name : indTercerResult.team1.name;
      if (individualStats[winner]) individualStats[winner].posicion = 3;
      if (individualStats[loser]) individualStats[loser].posicion = 4;
    }

    // 5º puesto: perdedor de primera ronda (D vs E)
    if (primeraRondaResult?.played) {
      const loser = primeraRondaResult.winner === 1 ? primeraRondaResult.team2.name : primeraRondaResult.team1.name;
      if (individualStats[loser]) individualStats[loser].posicion = 5;
    }

    // Ordenar por posición en bracket, luego por PG
    const individualRanking = Object.entries(individualStats)
      .map(([name, stats]) => ({ name, ...stats, diff: stats.setsF - stats.setsC }))
      .sort((a, b) => a.posicion - b.posicion || b.pg - a.pg || b.diff - a.diff);

    const individualRows = individualTable.querySelectorAll('tr');
    individualRows.forEach((row, i) => {
      const cells = row.querySelectorAll('td');
      if (individualRanking[i]) {
        const p = individualRanking[i];
        cells[1].textContent = p.name;
        cells[2].textContent = p.pj;
        cells[3].textContent = p.pg;
        cells[4].textContent = p.pp;
        cells[5].textContent = p.setsF;
        cells[6].textContent = p.setsC;
        cells[7].textContent = p.diff > 0 ? `+${p.diff}` : p.diff;
      } else {
        for (let j = 1; j < cells.length; j++) cells[j].textContent = '-';
      }
    });
  }

  document.querySelectorAll('[data-set]').forEach(input => {
    input.addEventListener('input', () => {
      updateFinalists();
      updateClasificacion();
    });
  });

  // === SINCRONIZACIÓN CON API ===
  const API_URL = '/api/resultados';
  const API_PROGRAMACION = '/api/programacion';
  let isSaving = false;
  let isSavingProgramacion = false;

  // Recopilar todos los datos del formulario (sin programación)
  function collectData() {
    const toggleVisible = document.getElementById('toggle-programacion-visible');
    const data = {
      dobles: {
        participantes: {},
        partidos: [],
        finalistas: {}
      },
      individual: {
        participantes: {},
        partidos: [],
        finalistas: {}
      },
      mostrarProgramacion: toggleVisible ? toggleVisible.checked : true
    };

    // Participantes dobles
    document.querySelectorAll('#dobles-femenino [data-participante]').forEach(input => {
      const key = input.dataset.participante.replace('dobles-', '');
      data.dobles.participantes[key] = input.value;
    });

    // Finalistas dobles (Gan. SF1, Gan. SF2)
    document.querySelectorAll('#dobles-femenino [data-finalist]').forEach(input => {
      const key = input.dataset.finalist.replace('dobles-', '');
      data.dobles.finalistas[key] = input.value;
    });

    // Participantes individual
    document.querySelectorAll('#individual-femenino [data-participante]').forEach(input => {
      const key = input.dataset.participante.replace('individual-', '');
      data.individual.participantes[key] = input.value;
    });

    // Finalistas individual (Gan. SF1, Gan. SF2)
    document.querySelectorAll('#individual-femenino [data-finalist]').forEach(input => {
      const key = input.dataset.finalist.replace('individual-', '');
      data.individual.finalistas[key] = input.value;
    });

    // Partidos dobles
    document.querySelectorAll('#dobles-femenino [data-match]').forEach(match => {
      const partido = { equipo1: [], equipo2: [] };
      match.querySelectorAll('[data-team="1"] [data-set]').forEach(input => {
        partido.equipo1.push(input.value);
      });
      match.querySelectorAll('[data-team="2"] [data-set]').forEach(input => {
        partido.equipo2.push(input.value);
      });
      data.dobles.partidos.push(partido);
    });

    // Partidos individual
    document.querySelectorAll('#individual-femenino [data-match]').forEach(match => {
      const partido = { equipo1: [], equipo2: [] };
      match.querySelectorAll('[data-team="1"] [data-set]').forEach(input => {
        partido.equipo1.push(input.value);
      });
      match.querySelectorAll('[data-team="2"] [data-set]').forEach(input => {
        partido.equipo2.push(input.value);
      });
      data.individual.partidos.push(partido);
    });

    return data;
  }

  // Recopilar datos de programación
  function collectProgramacion() {
    const programacion = [];
    document.querySelectorAll('#programacion-tbody tr').forEach(row => {
      const enfrentamiento = row.querySelector('.input-enfrentamiento')?.value || '';
      const modalidad = row.querySelector('.input-modalidad')?.value || '';
      const pista = row.querySelector('.input-pista')?.value || '';
      const hora = row.querySelector('.input-hora')?.value || '';
      programacion.push({ enfrentamiento, modalidad, pista, hora });
    });
    return programacion;
  }

  // Aplicar datos al formulario
  function applyData(data) {
    if (!data) return;

    // Participantes dobles
    if (data.dobles?.participantes) {
      Object.entries(data.dobles.participantes).forEach(([key, value]) => {
        const input = document.querySelector(`[data-participante="dobles-${key}"]`);
        if (input && input.value !== value) {
          input.value = value;
          document.querySelectorAll(`[data-show="dobles-${key}"]`).forEach(span => {
            span.textContent = value;
          });
        }
      });
    }

    // Finalistas dobles (Gan. SF1, Gan. SF2)
    if (data.dobles?.finalistas) {
      Object.entries(data.dobles.finalistas).forEach(([key, value]) => {
        const input = document.querySelector(`[data-finalist="dobles-${key}"]`);
        if (input && input.value !== value) {
          input.value = value;
        }
      });
    }

    // Participantes individual
    if (data.individual?.participantes) {
      Object.entries(data.individual.participantes).forEach(([key, value]) => {
        const input = document.querySelector(`[data-participante="individual-${key}"]`);
        if (input && input.value !== value) {
          input.value = value;
          document.querySelectorAll(`[data-show="individual-${key}"]`).forEach(span => {
            span.textContent = value;
          });
        }
      });
    }

    // Finalistas individual (Gan. SF1, Gan. SF2)
    if (data.individual?.finalistas) {
      Object.entries(data.individual.finalistas).forEach(([key, value]) => {
        const input = document.querySelector(`[data-finalist="individual-${key}"]`);
        if (input && input.value !== value) {
          input.value = value;
        }
      });
    }

    // Partidos dobles
    if (data.dobles?.partidos) {
      const doblesMatches = document.querySelectorAll('#dobles-femenino [data-match]');
      data.dobles.partidos.forEach((partido, i) => {
        if (!doblesMatches[i]) return;
        const inputs1 = doblesMatches[i].querySelectorAll('[data-team="1"] [data-set]');
        const inputs2 = doblesMatches[i].querySelectorAll('[data-team="2"] [data-set]');
        partido.equipo1.forEach((val, j) => { if (inputs1[j]) inputs1[j].value = val; });
        partido.equipo2.forEach((val, j) => { if (inputs2[j]) inputs2[j].value = val; });
      });
    }

    // Partidos individual
    if (data.individual?.partidos) {
      const individualMatches = document.querySelectorAll('#individual-femenino [data-match]');
      data.individual.partidos.forEach((partido, i) => {
        if (!individualMatches[i]) return;
        const inputs1 = individualMatches[i].querySelectorAll('[data-team="1"] [data-set]');
        const inputs2 = individualMatches[i].querySelectorAll('[data-team="2"] [data-set]');
        partido.equipo1.forEach((val, j) => { if (inputs1[j]) inputs1[j].value = val; });
        partido.equipo2.forEach((val, j) => { if (inputs2[j]) inputs2[j].value = val; });
      });
    }

    // Toggle de visibilidad de programación
    const toggleVisible = document.getElementById('toggle-programacion-visible');
    if (toggleVisible && data.mostrarProgramacion !== undefined) {
      toggleVisible.checked = data.mostrarProgramacion;
      updateToggleLabel();
    }

    calculateSetsWon();
    updateFinalists();
    updateClasificacion();
  }

  // Actualizar el label del toggle
  function updateToggleLabel() {
    const toggleVisible = document.getElementById('toggle-programacion-visible');
    const label = toggleVisible?.parentElement?.querySelector('.toggle-label');
    if (label) {
      label.textContent = toggleVisible.checked ? 'Visible' : 'Oculto';
    }
  }

  // === GESTIÓN DE PROGRAMACIÓN ===
  const programacionTbody = document.getElementById('programacion-tbody');
  const btnAddPartido = document.getElementById('btn-add-partido');
  let draggedRow = null;

  function createProgramacionRow(partido = {}) {
    const tr = document.createElement('tr');
    tr.draggable = true;
    tr.innerHTML = `
      <td class="drag-handle" title="Arrastrar para reordenar">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <line x1="4" y1="8" x2="20" y2="8"></line>
          <line x1="4" y1="16" x2="20" y2="16"></line>
        </svg>
      </td>
      <td><input type="text" class="prog-input input-enfrentamiento" placeholder="Ej: A vs B" value="${partido.enfrentamiento || ''}" /></td>
      <td><input type="text" class="prog-input input-modalidad" placeholder="Ej: Dobles F" value="${partido.modalidad || ''}" /></td>
      <td><input type="text" class="prog-input input-pista" placeholder="1" value="${partido.pista || ''}" /></td>
      <td><input type="text" class="prog-input input-hora" placeholder="10:00" value="${partido.hora || ''}" /></td>
      <td>
        <button class="btn-delete-row" title="Eliminar">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <line x1="18" y1="6" x2="6" y2="18"></line>
            <line x1="6" y1="6" x2="18" y2="18"></line>
          </svg>
        </button>
      </td>
    `;

    // Event listeners para drag & drop
    tr.addEventListener('dragstart', handleDragStart);
    tr.addEventListener('dragend', handleDragEnd);
    tr.addEventListener('dragover', handleDragOver);
    tr.addEventListener('dragleave', handleDragLeave);
    tr.addEventListener('drop', handleDrop);

    // Event listener para eliminar
    tr.querySelector('.btn-delete-row').addEventListener('click', () => {
      tr.remove();
      saveProgramacion();
    });

    // Event listeners para inputs
    tr.querySelectorAll('.prog-input').forEach(input => {
      input.addEventListener('focus', () => { isInputFocused = true; });
      input.addEventListener('blur', () => {
        isInputFocused = false;
        saveProgramacion();
      });
      input.addEventListener('input', () => { hasChanges = true; });
    });

    return tr;
  }

  function handleDragStart(e) {
    draggedRow = this;
    this.classList.add('dragging');
    e.dataTransfer.effectAllowed = 'move';
  }

  function handleDragEnd(e) {
    this.classList.remove('dragging');
    document.querySelectorAll('#programacion-tbody tr').forEach(row => {
      row.classList.remove('drag-over');
    });
    draggedRow = null;
  }

  function handleDragOver(e) {
    e.preventDefault();
    e.dataTransfer.dropEffect = 'move';
    if (this !== draggedRow) {
      this.classList.add('drag-over');
    }
  }

  function handleDragLeave(e) {
    this.classList.remove('drag-over');
  }

  function handleDrop(e) {
    e.preventDefault();
    this.classList.remove('drag-over');
    if (draggedRow && this !== draggedRow) {
      const allRows = [...programacionTbody.querySelectorAll('tr')];
      const draggedIdx = allRows.indexOf(draggedRow);
      const targetIdx = allRows.indexOf(this);

      if (draggedIdx < targetIdx) {
        this.parentNode.insertBefore(draggedRow, this.nextSibling);
      } else {
        this.parentNode.insertBefore(draggedRow, this);
      }

      saveProgramacion();
    }
  }

  function applyProgramacion(programacion) {
    // Solo actualizar si hay cambios reales
    const currentRows = programacionTbody.querySelectorAll('tr');

    // Verificar si necesitamos actualizar
    if (currentRows.length === programacion.length) {
      let needsUpdate = false;
      currentRows.forEach((row, i) => {
        const p = programacion[i];
        if (row.querySelector('.input-enfrentamiento')?.value !== p.enfrentamiento ||
            row.querySelector('.input-modalidad')?.value !== p.modalidad ||
            row.querySelector('.input-pista')?.value !== p.pista ||
            row.querySelector('.input-hora')?.value !== p.hora) {
          needsUpdate = true;
        }
      });
      if (!needsUpdate) return;
    }

    // Limpiar y recrear filas
    programacionTbody.innerHTML = '';
    programacion.forEach(partido => {
      programacionTbody.appendChild(createProgramacionRow(partido));
    });
  }

  // Añadir nuevo partido
  btnAddPartido.addEventListener('click', () => {
    programacionTbody.appendChild(createProgramacionRow());
    saveProgramacion();
  });

  // Cargar datos de la API
  async function loadData() {
    try {
      const res = await fetch(API_URL + '?t=' + Date.now(), {
        cache: 'no-store'
      });
      if (res.ok) {
        const data = await res.json();
        applyData(data);
      }
    } catch (e) {
      console.log('Error cargando datos:', e);
    }
  }

  // Guardar datos en la API
  async function saveData() {
    if (isSaving) return;
    isSaving = true;
    try {
      const data = collectData();
      await fetch(API_URL, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(data)
      });
    } catch (e) {
      console.log('Error guardando datos:', e);
    }
    isSaving = false;
  }

  // Cargar programación de la API
  async function loadProgramacion() {
    try {
      const res = await fetch(API_PROGRAMACION + '?t=' + Date.now(), {
        cache: 'no-store'
      });
      if (res.ok) {
        const data = await res.json();
        applyProgramacion(data);
      }
    } catch (e) {
      console.log('Error cargando programación:', e);
    }
  }

  // Guardar programación en la API
  async function saveProgramacion() {
    if (isSavingProgramacion) return;
    isSavingProgramacion = true;
    try {
      const data = collectProgramacion();
      await fetch(API_PROGRAMACION, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(data)
      });
    } catch (e) {
      console.log('Error guardando programación:', e);
    }
    isSavingProgramacion = false;
  }

  // Control de foco para evitar actualizaciones mientras se edita
  let isInputFocused = false;
  let hasChanges = false;

  // Escuchar cambios en todos los inputs
  document.querySelectorAll('input').forEach(input => {
    input.addEventListener('focus', () => {
      isInputFocused = true;
    });

    input.addEventListener('blur', () => {
      isInputFocused = false;
      if (hasChanges) {
        saveData();
        hasChanges = false;
      }
    });

    input.addEventListener('input', () => {
      hasChanges = true;
      calculateSetsWon();
      updateFinalists();
      updateClasificacion();
    });
  });

  // Listeners específicos para inputs de finalistas (Gan. SF1, Gan. SF2)
  document.querySelectorAll('[data-finalist]').forEach(input => {
    input.addEventListener('blur', () => {
      saveData();
      updateClasificacion();
    });
  });

  // Event listener para toggle de visibilidad
  const toggleProgramacionVisible = document.getElementById('toggle-programacion-visible');
  if (toggleProgramacionVisible) {
    toggleProgramacionVisible.addEventListener('change', () => {
      updateToggleLabel();
      saveData();
    });
  }

  // Cargar datos al iniciar
  loadData();
  loadProgramacion();

  // Actualizar cada 2 segundos solo si no hay input enfocado
  setInterval(() => {
    if (!isInputFocused) {
      loadData();
      loadProgramacion();
    }
  }, 2000);
</script>

<style>
  h2 {
    display: flex;
    align-items: center;
    justify-content: space-between;
    color: #fff;
    font-size: 1rem;
    font-weight: 600;
    margin: 0 0 25px 0;
    padding: 12px 20px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    border-radius: 8px;
    background: #fd3414;
  }

  .btn-print-section {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 32px;
    height: 32px;
    border: none;
    border-radius: 6px;
    background: rgba(255, 255, 255, 0.2);
    color: #fff;
    cursor: pointer;
    transition: background 0.2s;
  }

  .btn-print-section:hover {
    background: rgba(255, 255, 255, 0.3);
  }

  @media print {
    @page {
      size: landscape;
      margin: 10mm;
    }

    .btn-print-section {
      display: none !important;
    }

    body[data-print-section] .container > header,
    body[data-print-section] .info,
    body[data-print-section] .sin-participacion {
      display: none !important;
    }

    body[data-print-section="dobles-femenino"] #individual-femenino {
      display: none !important;
    }

    body[data-print-section="individual-femenino"] #dobles-femenino {
      display: none !important;
    }

    body[data-print-section] .modalidad {
      border: none !important;
      padding: 0 !important;
      margin: 0 !important;
    }

    body[data-print-section] h2 {
      background: none !important;
      color: #000 !important;
      padding: 0 0 10px 0 !important;
      border-bottom: 2px solid #000;
      border-radius: 0 !important;
    }
  }

  h3 {
    color: #1a1a1a;
    font-size: 0.85rem;
    font-weight: 600;
    margin: 30px 0 12px;
    text-transform: uppercase;
    letter-spacing: 0.3px;
  }

  .info {
    border: 1px solid #e5e5e5;
    padding: 16px 20px;
    border-radius: 8px;
    margin-bottom: 30px;
    text-align: center;
  }

  .info p {
    margin: 4px 0;
    font-size: 0.9rem;
    color: #444;
  }

  .modalidad {
    padding: 30px 0 40px 0;
    margin-bottom: 20px;
    border-bottom: 2px solid #e5e5e5;
  }

  .modalidad:last-of-type {
    border-bottom: none;
  }

  /* Participantes */
  .participantes-grid {
    display: flex;
    flex-direction: column;
    gap: 6px;
    margin-bottom: 10px;
  }

  .participante-tag {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 4px 0;
    font-size: 0.9rem;
  }

  .participante-input {
    flex: 1;
    border: none;
    border-bottom: 1px solid #e5e5e5;
    padding: 4px 0;
    font-size: 0.9rem;
    background: transparent;
    max-width: 300px;
  }

  .participante-input:focus {
    outline: none;
    border-bottom-color: #999;
  }

  .letra {
    background: #f5f5f5;
    color: #666;
    width: 22px;
    height: 22px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 4px;
    font-weight: 600;
    font-size: 0.8rem;
  }

  /* Bracket */
  .bracket-container {
    margin-top: 15px;
  }

  .bracket {
    display: flex;
    align-items: center;
    gap: 0;
    overflow-x: auto;
    padding: 10px 0;
  }

  .bracket-round {
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 30px;
  }

  .round-label {
    font-size: 0.7rem;
    text-transform: uppercase;
    color: #888;
    font-weight: 600;
    margin-bottom: 8px;
    text-align: center;
  }

  .modalidad-individual .bracket-round .round-label {
    margin-top: 0;
  }

  .match {
    border: 1px solid #e0e0e0;
    border-radius: 6px;
    overflow: hidden;
    width: 480px;
    background: #fff;
  }

  .modalidad-individual .match {
    width: 350px;
  }

  /* Dobles Femenino - alineación */
  .modalidad-dobles .bracket {
    align-items: flex-start;
  }

  .modalidad-dobles .bracket-round {
    justify-content: flex-start;
    align-self: flex-start;
  }

  .modalidad-dobles .round-label {
    position: relative;
    top: 0;
  }

  .modalidad-dobles .bracket-connector {
    height: 290px;
    margin-top: 28px;
    align-self: flex-start;
  }

  .modalidad-dobles .bracket-connector .connector-line.top {
    top: 81px;
  }

  .modalidad-dobles .bracket-connector .connector-line.bottom {
    top: 226px;
    bottom: auto;
  }

  .modalidad-dobles .bracket-connector .connector-merge {
    top: 81px;
    height: 145px;
  }

  .modalidad-dobles .bracket-round:nth-child(3) .match {
    margin-top: 72px;
  }

  .modalidad-individual .bracket {
    align-items: flex-start;
  }

  .modalidad-individual .bracket-round {
    justify-content: flex-start;
    align-self: flex-start;
  }

  .modalidad-individual .round-label {
    position: relative;
    top: 0;
  }

  .modalidad-individual .bracket-connector:not(.single) {
    height: 260px;
    margin-top: 28px;
    align-self: flex-start;
  }

  .modalidad-individual .bracket-connector:not(.single) .connector-line.top {
    top: 81px;
  }

  .modalidad-individual .bracket-connector:not(.single) .connector-line.bottom {
    top: 226px;
    bottom: auto;
  }

  .modalidad-individual .bracket-connector:not(.single) .connector-merge {
    top: 81px;
    height: 145px;
  }

  .modalidad-individual .bracket-round:last-of-type .match {
    margin-top: 72px;
  }

  .match-team {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 12px 15px;
    border-bottom: 1px solid #f0f0f0;
  }

  .match-team:last-child {
    border-bottom: none;
  }

  .team-name {
    font-size: 0.85rem;
    font-weight: 500;
    flex: 1;
  }

  .team-name-input {
    font-size: 0.85rem;
    font-weight: 500;
    flex: 1;
    border: none;
    border-bottom: 1px solid #e5e5e5;
    padding: 4px 0;
    background: transparent;
    min-width: 120px;
  }

  .team-name-input:focus {
    outline: none;
    border-bottom-color: #999;
  }

  .team-name-input::placeholder {
    color: #999;
    font-style: italic;
  }

  .score-total {
    margin-right: 8px;
  }

  .score-total span {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 26px;
    height: 26px;
    border: 1px solid #ddd;
    border-radius: 4px;
    background: #f9f9f9;
    font-size: 0.95rem;
    font-weight: 600;
    color: #333;
  }

  .score-boxes {
    display: flex;
    gap: 4px;
  }

  .score-input {
    width: 36px;
    height: 28px;
    border: 1px solid #ddd;
    border-radius: 4px;
    background: #fff;
    font-size: 0.9rem;
    font-weight: 600;
    text-align: center;
    color: #333;
  }

  .score-input:focus {
    outline: none;
    border-color: #999;
  }

  .score-input.third {
    border-style: dashed;
    opacity: 0.6;
  }

  .final-match {
    border: 2px solid #ddd;
  }

  .final-match .match-team {
    background: #fafafa;
  }

  /* Conectores */
  .bracket-connector {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    width: 50px;
    height: 180px;
    position: relative;
  }

  .bracket-connector.single {
    width: 30px;
    height: 1px;
    background: #ccc;
    margin-top: 108px;
    align-self: flex-start;
    flex-shrink: 0;
  }

  .bracket-connector.single .connector-line {
    display: none;
  }

  .connector-line {
    position: absolute;
    width: 25px;
    height: 1px;
    background: #ccc;
    left: 0;
  }

  .connector-line.top {
    top: 40px;
  }

  .connector-line.bottom {
    bottom: 40px;
  }

  .connector-line.middle {
    top: 50%;
  }

  .connector-merge {
    position: absolute;
    left: 25px;
    width: 1px;
    height: 100px;
    background: #ccc;
  }

  .connector-merge::after {
    content: '';
    position: absolute;
    right: -25px;
    top: 50%;
    width: 25px;
    height: 1px;
    background: #ccc;
  }

  .trophy {
    padding-left: 20px;
  }

  .trophy-icon {
    font-size: 2rem;
  }

  .trophy-icon.small {
    font-size: 1.5rem;
    margin-top: 10px;
  }

  /* Tercer puesto y puestos inferiores */
  .tercer-puesto,
  .quinto-puesto {
    margin-top: 30px;
    padding-top: 25px;
    border-top: 1px dashed #ddd;
  }

  .tercer-puesto .round-label,
  .quinto-puesto .round-label {
    text-align: center;
  }

  .modalidad-dobles .tercer-puesto .round-label {
    width: 480px;
  }

  .modalidad-individual .tercer-puesto .round-label,
  .modalidad-individual .quinto-puesto .round-label {
    width: 350px;
  }

  .puestos-inferiores {
    display: flex;
    gap: 30px;
    flex-wrap: wrap;
  }

  .bye-box {
    padding: 12px 15px;
    background: #f9f9f9;
    border: 1px dashed #ddd;
    border-radius: 6px;
    font-size: 0.8rem;
    color: #666;
    text-align: center;
    margin-top: 10px;
  }

  .bye-match {
    background: #f9f9f9;
  }

  .bye-match .match-team {
    justify-content: center;
    padding: 12px;
  }

  /* Tabla de clasificación */
  .clasificacion {
    width: 100%;
    max-width: 600px;
    border-collapse: collapse;
    font-size: 0.85rem;
    margin-top: 15px;
  }

  .clasificacion th,
  .clasificacion td {
    padding: 10px 12px;
    text-align: left;
    border-bottom: 1px solid #e5e5e5;
  }

  .clasificacion th {
    background: #f5f5f5;
    font-weight: 600;
    font-size: 0.75rem;
    text-transform: uppercase;
    color: #666;
  }

  .clasificacion td:first-child {
    font-weight: 600;
    color: #fd3414;
  }

  .clasificacion tbody tr:hover {
    background: #fafafa;
  }

  .clasificacion td:nth-child(n+3) {
    text-align: center;
  }

  .clasificacion th:nth-child(n+3) {
    text-align: center;
  }

  .sin-participacion {
    padding: 20px 0;
    color: #999;
    font-size: 0.85rem;
  }

  /* === PROGRAMACIÓN === */
  .programacion {
    padding: 30px 0 40px 0;
    margin-top: 20px;
    border-top: 2px solid #e5e5e5;
  }

  .programacion h2 {
    display: flex;
    align-items: center;
    justify-content: space-between;
    background: #2563eb;
  }

  .programacion-controls {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .toggle-visibility {
    display: flex;
    align-items: center;
    gap: 8px;
    cursor: pointer;
    user-select: none;
  }

  .toggle-visibility input {
    display: none;
  }

  .toggle-slider {
    width: 36px;
    height: 20px;
    background: rgba(255, 255, 255, 0.3);
    border-radius: 10px;
    position: relative;
    transition: background 0.2s;
  }

  .toggle-slider::after {
    content: '';
    position: absolute;
    width: 16px;
    height: 16px;
    background: #fff;
    border-radius: 50%;
    top: 2px;
    left: 2px;
    transition: transform 0.2s;
  }

  .toggle-visibility input:checked + .toggle-slider {
    background: rgba(255, 255, 255, 0.5);
  }

  .toggle-visibility input:checked + .toggle-slider::after {
    transform: translateX(16px);
  }

  .toggle-label {
    font-size: 0.75rem;
    font-weight: 400;
    text-transform: none;
    letter-spacing: 0;
    opacity: 0.9;
  }

  .btn-add-partido {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 32px;
    height: 32px;
    border: none;
    border-radius: 6px;
    background: rgba(255, 255, 255, 0.2);
    color: #fff;
    cursor: pointer;
    transition: background 0.2s;
  }

  .btn-add-partido:hover {
    background: rgba(255, 255, 255, 0.3);
  }

  .tabla-programacion-container {
    overflow-x: auto;
  }

  .tabla-programacion {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.85rem;
    margin-top: 15px;
  }

  .tabla-programacion th,
  .tabla-programacion td {
    padding: 12px 8px;
    text-align: left;
    border-bottom: 1px solid #e5e5e5;
    vertical-align: middle;
  }

  .tabla-programacion td {
    padding: 8px 6px;
  }

  .tabla-programacion th {
    background: #f5f5f5;
    font-weight: 600;
    font-size: 0.75rem;
    text-transform: uppercase;
    color: #666;
  }

  .tabla-programacion th.drag-handle-header,
  .tabla-programacion th.delete-header {
    width: 40px;
    padding: 10px 8px;
  }

  .tabla-programacion th:nth-child(4),
  .tabla-programacion th:nth-child(5),
  .tabla-programacion td:nth-child(4),
  .tabla-programacion td:nth-child(5) {
    text-align: center;
    width: 80px;
  }

  .tabla-programacion tbody :global(tr) {
    background: #fff;
    transition: background 0.15s;
  }

  .tabla-programacion tbody :global(tr:hover) {
    background: #fafafa;
  }

  .tabla-programacion tbody :global(tr.dragging) {
    opacity: 0.5;
    background: #e3f2fd;
  }

  .tabla-programacion tbody :global(tr.drag-over) {
    border-top: 2px solid #2563eb;
  }

  .tabla-programacion :global(.drag-handle) {
    cursor: grab;
    padding: 8px;
    color: #999;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .tabla-programacion :global(.drag-handle:active) {
    cursor: grabbing;
  }

  .tabla-programacion :global(.drag-handle svg) {
    width: 16px;
    height: 16px;
  }

  .tabla-programacion :global(.prog-input) {
    width: 100%;
    border: none;
    border-bottom: 1px solid #e5e5e5;
    padding: 4px 0;
    font-size: 0.85rem;
    background: transparent;
    border-radius: 0;
    -webkit-appearance: none;
    appearance: none;
  }

  .tabla-programacion :global(.prog-input:focus) {
    outline: none;
    border-bottom-color: #999;
  }

  .tabla-programacion :global(.prog-input::placeholder) {
    color: #bbb;
  }

  .tabla-programacion :global(.prog-input.input-pista),
  .tabla-programacion :global(.prog-input.input-hora) {
    text-align: center;
    width: 50px;
  }

  .tabla-programacion :global(.prog-input.input-hora) {
    width: 60px;
  }

  .tabla-programacion :global(.btn-delete-row) {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 28px;
    height: 28px;
    border: none;
    border-radius: 4px;
    background: transparent;
    color: #999;
    cursor: pointer;
    transition: all 0.2s;
  }

  .tabla-programacion :global(.btn-delete-row:hover) {
    background: #fee2e2;
    color: #dc2626;
  }

  .programacion-hint {
    margin-top: 12px;
    font-size: 0.75rem;
    color: #999;
    font-style: italic;
  }

  /* === ESTILOS MÓVILES === */
  @media (max-width: 800px) {
    .bracket {
      flex-direction: column;
      align-items: stretch;
      gap: 20px;
    }

    .bracket-connector {
      display: none;
    }

    .bracket-round {
      width: 100%;
      gap: 15px;
    }

    .round-label {
      background: #f5f5f5;
      padding: 8px 12px;
      border-radius: 4px;
      margin-bottom: 10px;
    }

    .match,
    .modalidad-individual .match {
      width: 100%;
      max-width: 100%;
    }

    .match-team {
      flex-wrap: wrap;
      gap: 8px;
      padding: 10px 12px;
    }

    .team-name {
      width: 100%;
      font-size: 0.9rem;
      margin-bottom: 4px;
    }

    .team-name-input {
      width: 100%;
      font-size: 0.9rem;
      margin-bottom: 4px;
    }

    .score-total {
      margin-right: 0;
    }

    .score-boxes {
      margin-left: auto;
    }

    .score-input {
      width: 38px;
      height: 32px;
      font-size: 1rem;
    }

    .score-total span {
      width: 30px;
      height: 30px;
      font-size: 1rem;
    }

    .modalidad-dobles .bracket-round:nth-child(3) .match,
    .modalidad-individual .bracket-round:last-of-type .match {
      margin-top: 0;
    }

    .tercer-puesto,
    .quinto-puesto {
      margin-top: 20px;
      padding-top: 20px;
    }

    .modalidad-dobles .tercer-puesto .round-label,
    .modalidad-individual .tercer-puesto .round-label,
    .modalidad-individual .quinto-puesto .round-label {
      width: 100%;
    }

    .puestos-inferiores {
      flex-direction: column;
      gap: 15px;
    }

    .bye-box {
      max-width: 100%;
    }

    /* Tabla clasificación móvil */
    .clasificacion {
      font-size: 0.75rem;
    }

    .clasificacion th,
    .clasificacion td {
      padding: 8px 6px;
    }

    .clasificacion th:nth-child(n+3),
    .clasificacion td:nth-child(n+3) {
      padding: 8px 4px;
    }

    /* Participantes móvil */
    .participante-input {
      max-width: 100%;
    }

    /* Info móvil */
    .info {
      padding: 12px 15px;
    }

    .info p {
      font-size: 0.85rem;
    }

    h2 {
      font-size: 0.9rem;
      padding: 10px 15px;
    }

    h3 {
      font-size: 0.8rem;
      margin: 20px 0 10px;
    }

    .modalidad {
      padding: 20px 0 30px 0;
    }

    .btn-print-section {
      display: none;
    }

    /* Programación móvil */
    .programacion {
      padding: 20px 0 30px 0;
    }

    .tabla-programacion th,
    .tabla-programacion td {
      padding: 6px 4px;
      font-size: 0.75rem;
    }

    .tabla-programacion .prog-input {
      font-size: 0.8rem;
    }

    .tabla-programacion .prog-input.input-pista,
    .tabla-programacion .prog-input.input-hora {
      width: 45px;
    }

    .tabla-programacion .prog-input.input-hora {
      width: 55px;
    }

    .tabla-programacion .drag-handle {
      padding: 4px;
    }

    .tabla-programacion th.drag-handle-header,
    .tabla-programacion th.delete-header {
      width: 32px;
    }
  }

  /* Móviles pequeños */
  @media (max-width: 400px) {
    .score-input {
      width: 32px;
      height: 28px;
    }

    .score-total span {
      width: 26px;
      height: 26px;
    }

    .match-team {
      padding: 8px 10px;
    }

    .team-name,
    .team-name-input {
      font-size: 0.8rem;
    }

    .clasificacion {
      font-size: 0.7rem;
    }

    .clasificacion th,
    .clasificacion td {
      padding: 6px 3px;
    }
  }
</style>
